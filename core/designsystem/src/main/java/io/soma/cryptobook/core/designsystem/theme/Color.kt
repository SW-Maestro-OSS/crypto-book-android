package io.soma.cryptobook.core.designsystem.theme

import androidx.compose.ui.graphics.Color

// CoinViewer
val BrandPrimary = Color(0xFF5A5AE2)

val ScreenBackground = Color(0xFF1A1A1A)
val CardBackground = Color(0xFF1E2128)

// TextPrimary, TextSecondary moved to Figma variables section below

val IconPrimary = Color(0xFFF3F4F6)
val Selectedicon = Color(0xFF0D59F2)

val PriceUp = Color(0xFF22C55E)
val PriceDown = Color(0xFFE11919)
val PriceFlat = Color(0xFFFAFAFA)

val SelectionActiveBackground = Color(0xFF0D59F2)
val SelectionBackground = Color(0xFF262A33)

// Figma Variables - Settings Screen
// Figma: color/text/primary
val textPrimary = Color(0xFFF3F4F6)

// Figma: color/text/secondary
val textSecondary = Color(0xFFBDC1CA)

// Figma: color/text/control/onSelected
val textControlOnSelected = Color(0xFFFFFFFF)

// Figma: color/text/control/default
val textControlDefault = Color(0xFFF3F4F6)

// Figma: color/surface/control/selected
val surfaceControlSelected = Color(0xFF0D59F2)

// Figma: color/surface/control/default
val surfaceControlDefault = Color(0xFF262A33)

// Figma: color/surface/card/default
val surfaceCardDefault = Color(0xFF1E2128)